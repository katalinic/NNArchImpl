Reinforcement learning library. Implementations to be added over time. Uses Open AI gym.

Initial implementations to be done in numpy, then ported to Tensorflow.

Presently contains:
Monte Carlo Policy Gradients
Actor Critic
